<h2>Edit editor</h2>

<%= render "form.html", changeset: @changeset, conn: @conn,
                        action: editor_path(@conn, :update, @transscript) %>

<%= link "Back", to: editor_path(@conn, :index) %>
